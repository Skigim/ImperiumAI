/**
 * Role Behaviors
 * 
 * High-level creep roles that orchestrate creep behavior.
 * Each role represents a strategic job assignment (e.g., harvester, upgrader, builder).
 * Roles delegate to specific tasks (see ../tasks) for individual action execution.
 * 
 * Pure functions for creep behavior by role.
 * Each role module exports a run function and context interface.
 */

// TODO: Export role behaviors

